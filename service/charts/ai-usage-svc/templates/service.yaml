apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-usage-svc.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-usage-svc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-usage-svc.selectorLabels" . | nindent 4 }}
